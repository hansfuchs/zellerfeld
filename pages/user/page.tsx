import { PageProps } from "@/types/next/page.types";

export default async function Profile({ params, searchParams }: PageProps) {
    return (
        <>
            <div className="flex">damn</div>
        </>
    );
}
